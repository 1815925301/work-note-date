DATA_DIR=./data
PORT=3001

# 本地开发：指向 Express 后端
VITE_API_BASE=http://localhost:3001

# Netlify 部署：留空即可，前端会请求同域 /api/*
# VITE_API_BASE=

# 访问密码（设置后启用登录保护；Netlify 上必须在 Environment variables 里配置）
APP_PASSWORD=your-password

# 可选：用于签发登录 token，建议与 APP_PASSWORD 不同
# APP_SECRET=随机字符串
